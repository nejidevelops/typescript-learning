//  Type annotations - Allow a user to specify the variable type
let awesomeName:string = 'shakeAndBake'

let names: string = 'Newton Jillian'
names = names.toUpperCase()

let age: number = 15
age = age + 5;

let isAdult: boolean = age >= 18;
isAdult = !isAdult;

